F.R.I.E.N.D. Root 10 Fixed

Open index.html from a web server. The Temperature Compliance tile on the Receiving dashboard and Food Safety/Temperature buttons now open the three-screen temperature-log route:
  templog1 -> templog2 -> templog3
All visible navigation, assistant, microphone, alerts, back/next controls, temperature workflow, notes, and selector controls have active behavior.

Note: templog1.webp was not included in the supplied files, so this package uses templog2.webp as a safe visual fallback for templog1 while preserving the complete three-step route.
